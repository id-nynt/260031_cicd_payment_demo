# Action boundary

The framework owns action policy, allow-listing, retry budgets, and the shell
executor abstractions. Concrete deployment/build scripts belong to the host
application and are intentionally not copied here.
