# Before the four-source migration (22 September 2026)

These are archival copies taken before editing the working primary inputs. They preserve the original payment pipeline, goals, workflow, agent and generation manifest, plus the reporting inputs and both resolved contracts.

Do not use this old generation manifest for a new campaign: it records the old two-input generator revision. Tests compare the resolved contracts and payment agent with the migrated implementation. Current inputs live under `bdi-cicd-framework/models/` and `bdi-cicd-framework/config/`.
